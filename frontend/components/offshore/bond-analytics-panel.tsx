import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"

const METRICS: Array<[string, string, string]> = [["clean_price","Clean price","par 100"],["dirty_price","Dirty price","par 100"],["accrued_interest","Accrued","par 100"],["ytm","YTM","% p.a."],["current_yield","Current yield","% p.a."],["macaulay_duration","Macaulay duration","ans"],["modified_duration","Modified duration","ans"],["convexity","Convexity","ans²"],["dv01_per_100","DV01 / 100","devise/bp"],["dv01_position","DV01 position","devise/bp"]]

export function BondAnalyticsPanel({ results = {} }: { results?: Record<string, unknown> }) { return <Card><CardHeader><CardTitle>Analytics</CardTitle></CardHeader><CardContent><div className="grid gap-3 sm:grid-cols-2 lg:grid-cols-5">{METRICS.map(([key,label,unit]) => { const raw=results[key]; const value=typeof raw === "number" ? ((key === "ytm" || key === "current_yield") ? raw*100 : raw) : null; return <div key={key} className="rounded-lg border p-3" title={key.includes("dv01") ? "Variation approximative de valeur pour un mouvement parallèle de 1bp." : `${label}, calculé par discounted cash flows.`}><div className="text-xs text-muted-foreground">{label}</div><div className="mt-1 text-lg font-semibold tabular-nums">{value == null ? "—" : value.toFixed(4)}</div><div className="text-xs text-muted-foreground">{unit}</div></div>})}</div></CardContent></Card> }
