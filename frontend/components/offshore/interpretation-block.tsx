import { Alert, AlertDescription, AlertTitle } from "@/components/ui/alert"
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"

export function InterpretationBlock({ methodology, assumptions = [], warnings = [], interpretation }: { methodology?: string; assumptions?: string[]; warnings?: string[]; interpretation?: string }) { return <Card><CardHeader><CardTitle>Interprétation et transparence</CardTitle></CardHeader><CardContent className="space-y-4 text-sm"><p>{interpretation ?? "Lancez un calcul pour générer une interprétation chiffrée."}</p><div><div className="font-medium">Méthodologie</div><p className="text-muted-foreground">{methodology ?? "Discounted cash flows et risk measures analytiques."}</p></div><div><div className="font-medium">Hypothèses</div><ul className="list-disc pl-5 text-muted-foreground">{assumptions.map((item)=><li key={item}>{item}</li>)}</ul></div>{warnings.map((warning)=><Alert key={warning}><AlertTitle>Warning</AlertTitle><AlertDescription>{warning}</AlertDescription></Alert>)}</CardContent></Card> }
